V29 — Crisp Mobile Scanner Labels

Fixes:
- Scanner text projected over buildings uses a 1280x320 canvas texture on mobile instead of 512x128.
- Uses native mobile/system font rasterization for scanner textures.
- Disables CanvasTexture mipmaps to prevent soft downsampling during orthographic zoom.
- Uses linear min/mag filtering plus supported anisotropic filtering.
- Slightly increases mobile scanner label world size for readability.
- Desktop scanner path keeps the lightweight 512x128 texture and original sizing.
- Cache/assets bumped to V29 so older V28 scanner textures are not reused.
